# requirements.txt — palm_payment
# Digenerate manual berdasarkan import di: app.py, verify.py,
# payment_routes.py, palm_payment_db.py, models.py

# --- Web framework ---
Flask==3.0.3

# --- Computer vision & numerik ---
opencv-python-headless==4.10.0.84   # headless karena jalan di server/Pi tanpa GUI
numpy==1.26.4

# --- Inference model (.tflite) ---
# Utamakan tflite-runtime (ringan, khusus inference).
# Kalau target platform tidak punya wheel tflite-runtime yang cocok,
# verify.py otomatis fallback ke tensorflow (lihat try/except di file itu) —
# dalam kasus itu, comment baris tflite-runtime dan uncomment tensorflow.
tflite-runtime==2.14.0
# tensorflow==2.16.1

# --- Database ---
psycopg2-binary==2.9.9   # versi -binary dipakai supaya tidak perlu compile dari source (butuh libpq-dev kalau pakai psycopg2 biasa)
SQLAlchemy==2.0.32        # dipakai models.py (skema lama, 8 tabel) — hapus baris ini kalau models.py sudah tidak dipakai lagi

# --- Khusus Raspberry Pi (JANGAN install lewat pip biasa) ---
# picamera2 dipakai di app.py, tapi package ini terikat ke libcamera
# sistem operasi Raspberry Pi OS dan TIDAK reliable kalau di-pip-install
# di venv biasa. Install lewat apt, lalu buat venv dengan akses ke
# system site-packages:
#
#   sudo apt install -y python3-picamera2
#   python3 -m venv venv --system-site-packages
#
# Kalau tetap ingin coba lewat pip (berisiko gagal build di banyak sistem):
# picamera2==0.3.20
